import { SkillNode } from "../SkillNode";

interface ISkillTreeAlternateJewelSettings {
    node: SkillNode;
    size: string;
    factionId: number;
    seed: string;
    name: string;
    extra_data: any;
}